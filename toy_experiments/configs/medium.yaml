name: medium
generator: toy

K_values: [15, 30]
N_values: [5, 10]
L_values: [5, 10]
T_values: [7]

delta_stab_values: [0.2]
delta_val_values: [0.2]
target_bias_values: [0.3]

seed: 0
budget_max: 30

influence_mode: dense
stability_competitor_mode: all
objective_family: full
make_budget_curves: true

solver:
  gurobi_threads: 8

output_dir: toy_experiments/outputs/medium/results
