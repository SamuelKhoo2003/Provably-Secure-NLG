name: smoke
generator: toy

K_values: [4]
N_values: [1]
L_values: [2]
T_values: [3]

delta_stab_values: [0.2]
delta_val_values: [0.2]
target_bias_values: [0.3]

seed: 0
budget_max: 4

influence_mode: dense
stability_competitor_mode: all
objective_family: full
make_budget_curves: false

solver:
  gurobi_threads: 0

output_dir: toy_experiments/outputs/smoke/results
