name: large
generator: toy

K_values: [50, 100]
N_values: [25, 50]
L_values: [20]
T_values: [25]

delta_stab_values: [0.25]
delta_val_values: [0.25]
target_bias_values: [0.3]

seed: 0
budget_max: 100

influence_mode: dense
stability_competitor_mode: all
objective_family: full
make_budget_curves: true

solver:
  gurobi_threads: 0

output_dir: toy_experiments/outputs/large/results
