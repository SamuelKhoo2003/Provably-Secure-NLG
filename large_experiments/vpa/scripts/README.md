# VPA Scripts

Place lightweight helper scripts for cluster discovery and smoke exports here.

Scripts should keep real inference opt-in, sequential, and limited until the
vote artifact schema is validated on real adapters.
