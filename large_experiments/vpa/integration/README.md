# VPA Integration Package

Import path:

```python
large_experiments.vpa.integration
```

Use this package for schemas, mock exports, validation, discovery, metadata, and
real VPA smoke exports behind `--enable-real-inference`.
